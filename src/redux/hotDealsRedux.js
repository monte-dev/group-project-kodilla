/* selectors */
export const getHotDeals = ({ hotDeal }) => hotDeal;

/* reducer */
export default function reducer(statePart = [], action = {}) {
  switch (action.type) {
    default:
      return statePart;
  }
}
